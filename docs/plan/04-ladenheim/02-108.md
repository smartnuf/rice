# 04-ladenheim / 02 — Compare with canonical 108-network Ladenheim catalogue

Status: `prog`

## Goal

Starting from the reproduced 148 structural catalogue, compare repository
classes with the canonical 108-network Ladenheim catalogue. The 108 are a subset
of the 148 after forty further exclusions, and their members satisfy `R <= 3`,
`L+C <= 2`, and `R+L+C <= 5`.

## Tasks

- Define the admissible star-delta contract before applying Y-to-delta or
  delta-to-Y moves.
- Apply the star-delta augmentation to the RICE local series/parallel track and
  to the colour-preserving 2-isomorphism track.
- Compare the resulting classes with the 108 catalogue.
- Record the forty exclusions from 148 to 108: eight four-resistor/one-reactive
  networks whose bilinear impedances have simpler realizations; four
  four-element networks reducible by Zobel transformation; twenty five-element
  series-parallel networks reducible by Zobel transformation; and eight further
  O/O-dual and bridge cases excluded using the source's Cauer-Foster,
  regularity, realizability, and related Y-delta reasoning.
- Identify any additional transformations, rejection criteria, or realizability
  arguments needed if star-delta alone does not account for the forty changes
  from 148 to 108.
- Verify candidate explanations against individual catalogue members, including
  source categories such as simpler bilinear realizations, Zobel
  transformations, Cauer-Foster relationships, regularity arguments, and related
  Y-delta cases.

## Done means

- The 108 comparison is supported by per-entry evidence rather than an
  unexplained stronger canonicalisation step.
- Star-delta effects are separated from other electrical-network transformations
  and realizability arguments.
- Any residual gap from the augmented structural classes to 108 is listed with
  concrete examples.

## Progress notes

- Added a deterministic two-layer evidence ledger: manually reviewed
  annotations in `data/comparisons/ladenheim-108-annotations.json`, generated
  148-row output in `data/comparisons/ladenheim-148-to-108.json`, and the human
  evidence contract in `docs/comparisons/ladenheim-148-to-108.md`.
- Refined the annotation contract to format version 2. Sources now describe
  origins only; precise authoritative evidence records, previous-workspace
  records, and computational cross-checks occupy separately validated
  namespaces. Morelli and Smith Section 5.1 is located at printed page 42 / PDF
  index 48 for the aggregate eight-entry statement.
- Evidence records now carry structured claims, and validation requires an
  exact subject/category/selector/count match rather than accepting provenance
  alone. The generated target is likewise tied to authoritative evidence for
  both `148 - 40 = 108` and the `8 + 4 + 20 + 8` category totals.
- Retention now requires positive, entry-specific authoritative evidence;
  exclusion counts are bounded by the declared total and category targets; and
  future graph assignments require separate historical-definition and
  subject-bound RICE-match evidence.
- The aggregate eight-entry evidence now carries its defining `r=4`, `lc=1`
  predicate; all evidence subjects are checked against the committed catalogue,
  and generation is restricted to its exact structural relation.
- Unresolved rows cannot carry positive dispositions, and all individual-record
  claim values and unstable timestamp metadata keys are validated on load.
- The version 2 contract is now closed-world across annotation objects and
  validates assertion tuples, identifier schemes, references, target bounds,
  and generated-ledger invariants before emitting the partial comparison.
- The eight `R=4`, `L+C=1` records are proposed exclusions only through the
  cited aggregate category plus a logically unique catalogue component-count
  match. They have no invented historical numbers or figure mappings.
- The other 140 dispositions remain unresolved. In particular, none of the 4,
  20, or final 8 exclusion groups has been assigned to individual records. The
  canonical 108 catalogue has not been reproduced, so this task remains
  `prog`.
- Added a focused four-element Zobel evidence report. It independently
  transcribes basic graphs `G`/`G^d` and canonical networks #15/#17, records all
  22 initial structural candidates, narrows them to exactly four RICE records,
  and checks their Zobel reductions algebraically. Applying those mappings to
  the production ledger and resolving the other 136 rows remain separate work,
  so this task remains `prog`.
- Applied the four independently checked four-element Zobel mappings to the
  production ledger as `derived-unique-match` exclusions. The ledger now has 12
  excluded and 136 unresolved rows; the twenty five-element Zobel and eight
  other canonical exclusions remain unmapped, and the canonical 108 catalogue
  has not been reproduced, so this task remains `prog`.
- Investigated the five-element Zobel exclusions with basic graph `L`. A focused
  report independently establishes all four RICE candidates and their mappings
  to targets #20, #25, #28, and #32 through colored structural matching and
  exact Zobel checks. These findings have not yet been applied to the production
  ledger; the remaining sixteen five-element Zobel mappings and eight final
  exclusions are still unresolved, so this task remains `prog`.
- Added format-version 3 support for complete authoritative graph-family groups
  joined to explicit subject-bound RICE graph and reduction-target matches. The
  route enforces complete population, one common authoritative graph fixture,
  exact target-set allocation, and a common independently reproduced
  computation scoped to the complete subject and target sets, without
  broadening component selectors or weakening existing evidence paths. The
  contract change itself did not apply the graph-`L` findings, so production at
  that stage remained 12 excluded, 136 unresolved, and 0 retained and this task
  remained `prog`.
- Applied the four reviewed graph-`L` five-element Zobel exclusions through the
  complete format-version 3 subject-bound route. Production now has 16 excluded,
  132 unresolved, and 0 retained rows; sixteen five-element Zobel mappings and
  eight final exclusions remain unresolved, with no graph assignments or
  historical identifiers populated, so this task remains `prog`.
- Investigated the single graph-`M` five-element Zobel exclusion in a focused
  evidence report. The complete 85-record census independently establishes one
  `R3L1C1` candidate and checks its reduction to canonical network #72 through
  the published Zobel identity and a resistor merge. The finding has not yet
  been applied: production remains 16 excluded, 132 unresolved, and 0 retained;
  a later graph-`M` application would leave fifteen five-element Zobel and eight
  final exclusions unresolved, so this task remains `prog`.
- Applied the reviewed graph-`M` exclusion through a complete one-member format-
  version 3 group. Production now has 17 excluded, 131 unresolved, and 0 retained
  rows; fifteen five-element Zobel mappings and eight final exclusions remain
  unresolved. All graph assignments and historical identifiers remain
  unpopulated, so this task remains `prog`.
- Completed an evidence-only investigation of the single graph-`M^d`
  five-element Zobel exclusion. The complete 85-record census independently
  identifies `lh148-635770ede187bca9` and checks its reduction to canonical
  network 73. This report does not apply the finding: production remains 17
  excluded, 131 unresolved, and 0 retained, so all fifteen five-element Zobel
  exclusions that were unapplied at the start remain unapplied. Graph
  assignments and historical identifiers remain unpopulated; a separate
  reviewed production application is required, and this task remains `prog`.
- Applied the reviewed graph-`M^d` exclusion through a complete one-member
  format-version 3 group. Production now has 18 excluded, 130 unresolved, and 0
  retained rows; fourteen five-element Zobel mappings and eight final
  exclusions remain unresolved. All graph assignments and historical
  identifiers remain unpopulated, so this task remains `prog`.
- Completed an evidence-only investigation of the four graph-`L^d`
  five-element Zobel exclusions. The complete 85-record census independently
  identifies `lh148-5d5fb848a810c9cb`, `lh148-534a4f02579831e9`,
  `lh148-9e140322bfd33a22`, and `lh148-a1e6c042c7d77e41` and checks their
  respective reductions to canonical networks 43, 47, 39, and 35. This report
  does not apply the findings: production remains 18 excluded, 130 unresolved,
  and 0 retained, so fourteen five-element Zobel mappings and eight final
  exclusions remain unresolved. Graph assignments and historical identifiers
  remain unpopulated; a separate reviewed production application is required,
  and this task remains `prog`.
- Applied the four reviewed graph-`L^d` exclusions through one complete
  format-version 3 group, preserving each report pathway from coloured source
  topology through the Figure 5.2 coefficient map and parallel-resistor merge
  to graph `F^d` and its canonical target. Production now has 22 excluded, 126
  unresolved, and 0 retained rows; ten graph-`S`/`S^d` five-element Zobel
  mappings and eight final exclusions remain unresolved. All graph assignments
  and historical identifiers remain unpopulated, so this task remains `prog`.
- Completed an evidence-only investigation of the five graph-`S`
  five-element Zobel exclusions. The complete 85-record census finds all 15
  graph-`S` assignments and independently identifies
  `lh148-dffc6c65dc65c0ec`, `lh148-5bdd1d37b2007a4c`,
  `lh148-3a7ebfebce0db0a4`, `lh148-53370c9917eea4d0`, and
  `lh148-45d19cefc5b496ce` as the five Zobel subjects, with respective checked
  reductions to canonical networks 22, 24, 30, 33, and 73. This report does
  not apply the findings: production remains 22 excluded, 126 unresolved, and
  0 retained. Graph assignments and historical identifiers remain
  unpopulated; a separate reviewed production application is required, and
  this task remains `prog`.
- Applied the five reviewed graph-`S` exclusions through one complete
  format-version 3 group, preserving the four forward Figure 5.2 pathways to
  graph `G` and the inverse pathway to graph `H^d`, including their coefficient
  maps, merge steps, target fixtures, and exact computation. Production now has
  27 excluded, 121 unresolved, and 0 retained rows; five graph-`S^d`
  five-element Zobel mappings and eight final exclusions remain unresolved.
  All graph assignments and historical identifiers remain unpopulated, so this
  task remains `prog`.
- Completed an evidence-only investigation of the five graph-`S^d`
  five-element Zobel exclusions. The complete 85-record census finds all 15
  graph-`S^d` assignments and independently identifies
  `lh148-84fedcab1cc7f77d`, `lh148-81ffdfa53c07c484`,
  `lh148-4cf39db00710fb1c`, `lh148-3bf13e1a1ad41cd5`, and
  `lh148-aefb4b8fe01749c6` as the five Zobel subjects, with respective checked
  reductions to canonical networks 37, 40, 45, 48, and 72. This report does
  not apply the findings: production remains 27 excluded, 121 unresolved, and
  0 retained. Graph assignments and historical identifiers remain
  unpopulated; a separate reviewed production application is required, and
  this task remains `prog`.
- Applied the five reviewed graph-`S^d` exclusions through one complete
  format-version 3 group, preserving the four inverse Figure 5.2 pathways to
  graph `G^d` and the forward pathway on the composite series L-C arm to graph
  `H`, including their coefficient maps, merge steps, positive-finite domain
  arguments, target fixtures, and exact computation. Production now has 32
  excluded, 116 unresolved, and 0 retained rows. All twenty five-element Zobel
  exclusions are applied; only the final eight O/O-dual and bridge exclusions
  remain unresolved and require a separate goal. Graph assignments and
  historical identifiers remain unpopulated, so this task remains `prog`.
- Completed an evidence-only investigation and contract assessment for the
  final eight graph-`O`/`O^d` and bridge exclusions. A complete 148-record
  census independently identifies four coloured series-parallel subjects and
  their four Y--delta-equivalent bridge partners, checks the coefficient-zero
  conditions and simpler-realisation targets, and recommends a narrowly scoped
  nongeneric-containment evidence route for separate review. Nothing is yet
  applied: production remains 32 excluded, 116 unresolved, and 0 retained;
  graph assignments and historical identifiers remain unpopulated, so this
  task remains `prog`.
